{
  "name": "tse",
  "version": "0.0.0",
  "description": "",
  "main": "index.js",
  "devDependencies": {
    "beefy": "~1.1.0",
    "browserify": "~3.46.0"
  },
  "scripts": {
    "start": "beefy index.js:bundle.js --live --open",
    "publish": "current_branch=\"`git rev-parse --abbrev-ref HEAD`\"; git branch -D gh-pages; git checkout -b gh-pages; browserify index.js -o bundle.js; git add -A; git commit -m 'New Build'; git push -f origin gh-pages; git checkout $current_branch"
  },
  "author": "",
  "license": "BSD-2-Clause",
  "dependencies": {
    "extend": "~1.2.1",
    "looking-glass": "The-Source-Eternal/LookingGlass"
  }
}
